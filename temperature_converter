#ask for the temp in F
f_temp = int(input("What is the temperature in fahrenheit?"))

#convert f to c
c_temp = (f_temp-32)*(5/9)

#round to one decimal place
answer = round(c_temp, 1)

#tell user
print(answer, "degrees celsius")

#add a personal touch
if c_temp <= 0:
    print("Wow, freezing!")
elif c_temp >= 100:
    print("Wow, boiling!")
else:
    print("Thank you for asking!")
